import React from 'react';
import { render } from '@testing-library/react';

describe("<App />", () => {

    beforeEach( async () => {

    });

    test('initial test', () => {
        expect(1).toBe(1);
    })

});